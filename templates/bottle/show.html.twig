{% extends 'base.html.twig' %}

{% block title %}Bottle{% endblock %}

{% block body %}
    <h1>Bottle</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ bottle.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ bottle.name }}</td>
            </tr>
            <tr>
                <th>Year</th>
                <td>{{ bottle.year }}</td>
            </tr>
            <tr>
                <th>Origin</th>
                <td>{{ bottle.origin }}</td>
            </tr>
            <tr>
                <th>Vine</th>
                <td>{{ bottle.vine }}</td>
            </tr>
            <tr>
                <th>Enbottler</th>
                <td>{{ bottle.enbottler }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('bottle_index') }}">back to list</a>

    <a href="{{ path('bottle_edit', {'id': bottle.id}) }}">edit</a>

    {{ include('bottle/_delete_form.html.twig') }}
{% endblock %}
