<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>invoice</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #FEF6F6;
            margin: 0;
            padding: 0;
        }
        .invoice-container {
            background-color: #ffffff;
            box-shadow: 0px 2px 6px rgba(0, 0, 0, 0.1);
            max-width: 800px;
            margin: 50px auto;
            border-radius: 10px;
        }
        .header {
            background-color: #6F1125;
            color: #ffffff;
            padding: 20px;
            border-top-left-radius: 10px;
            border-top-right-radius: 10px;
            text-align: center;
        }
        .logo {
            max-width: 120px;
            height: auto;
        }
        .invoice-details {
            padding: 20px;
            color: #555555;
        }
        .invoice-details h1 {
            font-size: 24px;
            margin-bottom: 10px;
            color: #6F1125;
        }
        .info-item {
            margin-bottom: 10px;
        }
        .info-label {
            font-weight: bold;
            color: #6F1125;
        }
        .invoice-items {
            margin-top: 30px;
        }
        .item-row {
            display: flex;
            justify-content: space-between;
            margin-bottom: 10px;
            border-bottom: 1px solid #dddddd;
            padding-bottom: 10px;
        }
        .item-description,
        .item-quantity,
        .item-amount {
            /* flex: 1; */
            padding: 5px;
        }
        .totals {
            margin-top: 20px;
            text-align: right;
            font-size: 18px;
            color: #6F1125;
        }
        .table-info {
            width: 100%;
            margin-top: 20px;
            text-align: start;
        }
        .table-articles {
            width: 100%;
            border: 1px solid #6F1125;
            text-align: center;
        }
    </style>
</head>
<body>
    <div class="invoice-container">
        <div class="header">
            <img class="logo" src="data:image/png;base64, {{ logo }}" alt="Logo">
        </div>
        <div class="invoice-details">
            <h1>Facture</h1>
            <div class="info-item">
                <span class="info-label">Date :</span>
                <span>{{ invoice.createdAt|date('d/m/Y H:i') }}</span>
            </div>
            <div class="info-item">
                <span class="info-label">Numéro :</span>
                <span>{{ invoice.paymentId }}</span>
            </div>
            {# <div class="info-item">
                <div class="info-label">Société :</div>
                <div>WINE STOCKKER<br>1 rue de nul part<br>Téléphone : +1 234 567 890<br>Email : info@winestockker.com</div>
            </div>
            <div class="info-item">
                <div class="info-label">Client :</div>
                <div>{{ user.firstname ~ ' ' ~ user.lastname }}<br>Adresse du client<br>Email : {{ user.email }}</div>
            </div> #}
            <table class="table-info">
                <thead>
                    <tr>
                        <th class="info-label">Société:</th>
                        <th class="info-label">Client:</th>
                    </tr>
                </thead>
                <tbody>
                    <tr>
                        <td>
                            WINE STOCKKER<br>
                            1 rue de nul part<br>
                            00001 Paris<br>
                            Email : info@winestockker.com
                        </td>
                        <td>
                            {{ user.firstname ~ ' ' ~ user.lastname }}<br>
                            {{ userAddress.streetNumber ~ ' ' ~ userAddress.streetNumberExtension ~ ' ' ~
                                userAddress.streetType ~ ' ' ~ userAddress.streetName }}<br>
                            {{ userAddress.postalcode ~ ' ' ~ userAddress.city }}<br>
                            Email : {{ user.email }}
                        </td>
                    </tr>
                </tbody>
            </table>
            <div class="invoice-items">
                <h3 class="info-label">Achat</h3>
                <table class="table-articles">
                    <thead>
                        <tr>
                            <th>Abonnement</th>
                            <th>Quantité</th>
                            <th>Prix unitaire</th>
                        </tr>
                    </thead>
                    <tbody>
                        <tr>
                            <td>{{ subscription.name }}</td>
                            <td>x1</td>
                            <td>{{ subscription.price }} €</td>
                        </tr>
                    </tbody>
                </table>
                <div class="totals">
                    <p>Sous total : {{ invoice.amount }} €</p>
                    <p>TVA : 0 €</p>
                    <p class="info-label">Total : {{ invoice.amount }} €</p>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
