{% extends 'base.html.twig' %}

{% block title %}{{ parent() ~ ' - Réinitialisation de mot de passe' }}{% endblock %}

{% block body %}
<section class="mt-4">
    <div class="container py-3 h-100">
        <div class="row d-flex justify-content-center align-items-center h-100">
            <div class="col col-lg-8 mb-4 mb-lg-0">
                <div class="card mb-3">
                    <div class="row g-0">
                        <div class="col-md-4 gradient-custom text-center text-white">
                            <img src={{ asset('images/loop.png') }}
                            alt="Avatar" class="img-fluid my-5 w-75" />
                        </div>
                        <div class="col-md-8">
                            <div class="card-body p-4 h-100 d-flex flex-column justify-content-center">
                                {{ form_start(resetForm) }}
                                    <p class="text-center h1 fw-bold mb-5 mx-1 mx-md-4 mt-4">Réinitialiser votre mot de passe</p>
                                    <!-- New password -->
                                    <div class="form-outline mb-4 h5 w-100">
                                        <div class="d-flex align-items-center">
                                            <i class="fas fa-lock fa-lg me-3 fa-fw"></i>
                                            {{ form_widget(resetForm.plainPassword.first) }}
                                        </div>
                                        {% for errorItem in resetForm.plainPassword.first.vars.errors %}
                                            <div class="text-danger ms-5">{{ errorItem.message }}</div>
                                        {% endfor %}
                                        <p class="password-error mb-4 ms-5"></p>
                                    </div>
                                    <div class="form-outline mb-4 h5 w-100">
                                        <div class="d-flex align-items-center">
                                            <i class="fas fa-key fa-lg me-3 fa-fw"></i>
                                            {{ form_widget(resetForm.plainPassword.second) }}
                                            {% for errorItem in resetForm.plainPassword.second.vars.errors %}
                                                <div class="text-danger ms-5">{{ errorItem.message }}</div>
                                            {% endfor %}
                                            <p class="confirm-password-error mb-4 ms-5"></p>
                                        </div>
                                    </div>
                                    <!-- End new password -->
                                    <!-- Submit button -->
                                    <div class="w-100 d-flex justify-content-center">
                                        <button type="submit" value="submit" class="btn btn-purple">
                                            Valider
                                        </button>
                                    </div>
                                    <!-- End submit button -->
                                {{ form_end(resetForm) }}
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

{% endblock %}

{% block javascripts %}
    {{ parent() }}
    {{ encore_entry_script_tags('editPassword') }}
{% endblock %}