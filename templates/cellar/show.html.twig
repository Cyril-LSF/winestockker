{% extends 'base.html.twig' %}

{% block title %}Cellar{% endblock %}

{% block body %}
    <h1>Cellar</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cellar.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ cellar.name }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ cellar.createdAt ? cellar.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('cellar_index') }}">back to list</a>

    <a href="{{ path('cellar_edit', {'id': cellar.id}) }}">edit</a>

    {{ include('cellar/_delete_form.html.twig') }}
{% endblock %}
